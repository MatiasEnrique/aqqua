interface:
  display_name: "aqqua App Testing"
  short_description: "Launch and retain isolated aqqua test environments"
  default_prompt: "Use $test-aqqua-app to launch an isolated aqqua environment and iteratively test it in the browser while preserving state."
